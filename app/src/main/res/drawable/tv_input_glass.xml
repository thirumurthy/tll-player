<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    
    <!-- Focused state -->
    <item android:state_focused="true">
        <layer-list>
            <!-- Glass background -->
            <item>
                <shape>
                    <solid android:color="@color/glass_background_focused" />
                    <corners android:radius="8dp" />
                </shape>
            </item>
            
            <!-- Inner shadow effect -->
            <item android:top="1dp" android:left="1dp" android:right="1dp">
                <shape>
                    <gradient
                        android:startColor="@color/glass_overlay_light"
                        android:endColor="@android:color/transparent"
                        android:angle="90" />
                    <corners android:radius="7dp" />
                </shape>
            </item>
            
            <!-- Enhanced border for focus -->
            <item>
                <shape>
                    <stroke 
                        android:width="2dp" 
                        android:color="@color/glass_border_focused" />
                    <corners android:radius="8dp" />
                    <solid android:color="@android:color/transparent" />
                </shape>
            </item>
        </layer-list>
    </item>
    
    <!-- Normal state -->
    <item>
        <layer-list>
            <!-- Glass background -->
            <item>
                <shape>
                    <solid android:color="@color/glass_background" />
                    <corners android:radius="8dp" />
                </shape>
            </item>
            
            <!-- Inner shadow effect -->
            <item android:top="1dp" android:left="1dp" android:right="1dp">
                <shape>
                    <gradient
                        android:startColor="@color/glass_overlay_light"
                        android:endColor="@android:color/transparent"
                        android:angle="90" />
                    <corners android:radius="7dp" />
                </shape>
            </item>
            
            <!-- Subtle border -->
            <item>
                <shape>
                    <stroke 
                        android:width="1dp" 
                        android:color="@color/glass_border" />
                    <corners android:radius="8dp" />
                    <solid android:color="@android:color/transparent" />
                </shape>
            </item>
        </layer-list>
    </item>
    
</selector>