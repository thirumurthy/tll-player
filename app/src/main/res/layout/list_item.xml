<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/list_item"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="@dimen/glass_item_margin"
    android:layout_marginStart="@dimen/glass_item_margin"
    android:layout_marginEnd="@dimen/glass_item_margin"
    app:cardCornerRadius="@dimen/glass_corner_radius"
    app:cardElevation="@dimen/glass_card_elevation"
    app:cardBackgroundColor="@color/glass_card_background"
    android:focusable="true"
    android:clickable="true">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="@dimen/glass_card_padding">

        <androidx.cardview.widget.CardView
            android:id="@+id/icon_frame"
            android:layout_width="56dp"
            android:layout_height="56dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:cardCornerRadius="12dp"
            app:cardElevation="0dp"
            app:cardBackgroundColor="@color/glass_highlight">

            <ImageView
                android:id="@+id/icon"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:scaleType="centerCrop"
                android:padding="8dp"
                android:src="@drawable/ic_tv_placeholder"
                android:contentDescription="Channel Icon" />
        </androidx.cardview.widget.CardView>

        <TextView
            android:id="@+id/title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            app:layout_constraintStart_toEndOf="@id/icon_frame"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toStartOf="@+id/actions_container"
            android:layout_marginStart="20dp"
            android:layout_marginEnd="16dp"
            android:textColor="@color/glass_text_primary"
            android:maxLines="1"
            tools:text="Channel Name"
            android:ellipsize="end"
            android:textSize="@dimen/tv_text_size_medium"
            android:textStyle="bold"
            android:fontFamily="sans-serif-medium"/>

        <LinearLayout
            android:id="@+id/actions_container"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent">

            <ImageView
                android:id="@+id/heart"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:src="@drawable/ic_heart_empty"
                android:contentDescription="Favorite"
                android:clickable="true"
                android:focusable="false"
                android:padding="6dp"
                android:layout_marginEnd="8dp"
                app:tint="@color/glass_text_secondary"
                android:background="?android:attr/selectableItemBackgroundBorderless"/>

            <LinearLayout
                android:id="@+id/arrows"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:visibility="gone">

                <ImageView
                    android:id="@+id/arrow_up"
                    android:layout_width="24dp"
                    android:layout_height="24dp"
                    android:src="@android:drawable/arrow_up_float"
                    android:clickable="true"
                    android:focusable="true"
                    android:contentDescription="Move up"
                    app:tint="@color/glass_text_secondary"/>

                <ImageView
                    android:id="@+id/arrow_down"
                    android:layout_width="24dp"
                    android:layout_height="24dp"
                    android:src="@android:drawable/arrow_down_float"
                    android:clickable="true"
                    android:focusable="true"
                    android:contentDescription="Move down"
                    app:tint="@color/glass_text_secondary"/>
            </LinearLayout>
        </LinearLayout>

        <TextView
            android:id="@+id/description"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            app:layout_constraintStart_toEndOf="@id/icon_frame"
            app:layout_constraintTop_toBottomOf="@id/title"
            app:layout_constraintEnd_toStartOf="@+id/actions_container"
            android:layout_marginStart="20dp"
            android:visibility="gone"
            android:text=""
            android:textColor="@color/glass_text_secondary"
            android:textSize="@dimen/glass_text_size_secondary"
            android:maxLines="1"
            android:ellipsize="end" />
    </androidx.constraintlayout.widget.ConstraintLayout>

    <!-- Focus Overlay -->
    <View
        android:id="@+id/focus_overlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/item_focus_overlay"
        android:visibility="gone"/>

</androidx.cardview.widget.CardView>